# библиотека, которая позволяет взаимодействовать с операционной системой:
import os
# из библиотеки dotenv берем ф-цию load_dotenv, которая подгружает переменные окружения
# из файла .env в директории проекта
from dotenv import load_dotenv

# Вызываем функцию для подгрузки переменных окружения
load_dotenv()

# Получаем определенные значения из переменных окружения
TOKEN = str(os.getenv('TOKEN'))
ADMIN_ID = int(os.getenv('ADMIN_ID'))
